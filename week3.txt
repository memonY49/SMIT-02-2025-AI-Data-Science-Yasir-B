Nested If:
 	if condition:
		body of outer if 
		if condition:
			body of inner if
		else:
			body of inner else
	else:
		body of outer else

comments: 
	single line: #starts with hash
	multi line: """strats with inverted comas"""


List :
	"its is the collection of heterogenous elements"
	syntax:
		myList = [2,77,4,6,7,8,9]
		myList[index]
Tuple:
	"its is the collection of heterogenous elements tuples are immutable "
	syntax:
 		myTupple = (2,4,5,6,8,'g',true,4.0)
		myTupple[index]
Sets: 
	"its is the collection of distinct  heterogenous elements and it unorderd collecion"
	syntax:
		mySet = {2,4,5,2,6,8,2}

List Functions: 
	append( Element to add)	Adds an element at the end of the list
	clear()			Removes all the elements from the list
	copy()			Returns a copy of the list
	count(Element)		Returns the number of elements with the specified value
	extend()		Add the elements of a list (or any iterable), to the end of the current list
	index(Element)		Returns the index of the first element with the specified value
	insert(Index, Element)	Adds an element at the specified position
	pop()			Removes the element at the last element
	remove(Element)		Removes the item with the specified value
	reverse()		Reverses the order of the list
	sort()			Sorts the list





